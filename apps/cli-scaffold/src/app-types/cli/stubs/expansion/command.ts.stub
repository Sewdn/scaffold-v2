import { Command } from "commander";

export const {{commandExportName}}Command = new Command("{{commandName}}")
  .description("{{commandDescription}}")
  .action(async () => {
    // TODO: implement command logic
  });
